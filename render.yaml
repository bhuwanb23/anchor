# Anchor — Render Blueprint (free tier)
# Docs: docs/deploy-render.md
#
# Free limits (Render):
# - No persistent disks → SQLite is ephemeral (lost on sleep / redeploy)
# - Services sleep after ~15 min idle; cold start ~1 min
# - WebSockets work while the instance is awake
#
# Default URLs (rename services + update env if names are taken):
#   API:       https://anchor-api.onrender.com
#   Dashboard: https://anchor-web.onrender.com

services:
  - type: web
    name: anchor-api
    runtime: docker
    plan: free
    region: oregon
    dockerfilePath: ./control-plane/Dockerfile
    dockerContext: .
    healthCheckPath: /health
    numInstances: 1
    envVars:
      - key: ENV
        value: production
      - key: PORT
        value: "8080"
      # Ephemeral path — free tier cannot attach a disk
      - key: DATABASE_PATH
        value: /tmp/anchor/yourplatform.db
      - key: DB_BACKUP_DIR
        value: /tmp/anchor/backups
      - key: JWT_SECRET
        generateValue: true
      - key: JWT_EXPIRY_HOURS
        value: "24"
      - key: FRONTEND_URL
        value: https://anchor-web.onrender.com
      - key: PUBLIC_BASE_URL
        value: https://anchor-api.onrender.com
      - key: BASE_DOMAIN
        value: anchor-api.onrender.com
      - key: WS_PATH
        value: /ws/agent
      - key: EMAIL_ENABLED
        value: "false"

  - type: web
    name: anchor-web
    runtime: docker
    plan: free
    region: oregon
    dockerfilePath: ./dashboard/Dockerfile
    dockerContext: .
    healthCheckPath: /
    numInstances: 1
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: "3000"
      - key: NEXT_PUBLIC_API_URL
        value: https://anchor-api.onrender.com
      - key: NEXT_PUBLIC_WS_URL
        value: wss://anchor-api.onrender.com/ws/browser
